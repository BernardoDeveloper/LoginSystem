server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    password: 'Bernardo300'
    url: jdbc:postgresql://localhost:5432/loginsystem
    username: 'postgres'
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show-sql: true

  mail:
    host: smtp.mailtrap.io
    port: 2525
    username: 13ca40b64c02c3
    password: d69758a76501a1

    properties:
      mail:
        smtp:
          ssl:
            trust: '*'
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
